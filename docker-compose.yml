version: '3.8'

services:
  # Base de Datos PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: psicoeducandonos_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: psicoeducandonos_db
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Backend: NestJS
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: psicoeducandonos_backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - ./backend/.env
    depends_on:
      - db
    # Usamos esto para que recompile automágicamente cuando cambies un archivo
    command: npm run start:dev

  # Frontend: Angular
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: psicoeducandonos_frontend
    restart: unless-stopped
    ports:
      - '4200:4200'
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules
    # El flag --host 0.0.0.0 es crítico en docker para que el puerto sea accesible desde fura
    command: npm start -- --host 0.0.0.0 --poll 2000 

volumes:
  pgdata:
